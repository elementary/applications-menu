#!/usr/bin/make -f

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

%:
	dh $@ --buildsystem=meson

override_dh_missing:
	dh_missing --list-missing --fail-missing

override_dh_strip:
	dh_strip --dbgsym-migration='slingshot-launcher-dbg (<< 2.7.1)'
